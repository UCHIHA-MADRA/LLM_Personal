# Personal LLM — Dependencies
# These are the only things you need. No Ollama, no Docker, no cloud services.

# Core LLM engine (loads GGUF models directly)
llama-cpp-python>=0.3.0

# REST API backend
fastapi>=0.100
uvicorn>=0.20
python-multipart>=0.0.22
pydantic>=2.0

# HTTP client (cloud proxy, model downloads)
requests>=2.28.0
httpx>=0.24.0

# Model downloader (one-time use, then offline)
huggingface-hub>=0.20.0

# Web UI (legacy standalone)
gradio>=4.0.0

# RAG / Knowledge Base
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Document Parsers (optional but recommended for RAG)
PyPDF2>=3.0.0
python-docx>=1.1.0
openpyxl>=3.1.2
python-pptx>=0.6.23

# System monitoring
psutil>=5.9.0

# Analytics sync (optional, for MongoDB telemetry)
pymongo>=4.0.0
